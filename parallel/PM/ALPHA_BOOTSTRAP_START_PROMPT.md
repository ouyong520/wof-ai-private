# WOF Alpha Normal-User Bootstrap Audit — canonical launcher

This file exists as the canonical short-path launcher for the dedicated Alpha bootstrap work thread.

Read and follow the complete current instructions in:

`parallel/PM/ALPHA_BOOTSTRAP_AUDIT_START_PROMPT.md`

Then continue until the stop condition in that file is reached. Treat `product/alpha/**` as read-only and write findings only under `parallel/ALPHABOOT/**`.
