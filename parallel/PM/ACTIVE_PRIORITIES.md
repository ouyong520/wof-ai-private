# WOF Future Danger AI — Active Priorities

Updated: 2026-09-01 — second PM audit

## P0 — PRODUCT / ALPHA release engineering

Start the bounded product implementation workstream defined by:

- `parallel/PM/ALPHA_FREEZE_SPEC.md`
- `parallel/PM/ALPHA_ENGINEERING_TASKS.md`
- `parallel/PM/PRODUCT_ALPHA_START_PROMPT.md`

Highest-value product tasks now are:

1. inventory/reuse existing WebGL HUD and production danger-map assets;
2. build exact frozen release manifest from audited Browser predicates;
3. isolate release runtime from discovery/coordinator instrumentation;
4. add positive runtime/build identity guard and fail-closed behavior;
5. preserve live target/retarget and current side recomputation;
6. run release-artifact regression;
7. produce Alpha RC.

Do not wait for WOF-052 or full attack coverage to do this work.

## P0 — MAINLINE WOF-052 ordered T18 discrimination

The exact T18 BODY4728/A4/B2/TM1 anchor is prospectively attack-ambiguous. WOF-052 coordinator code already exists; the remaining gate is Browser data.

After the run:
- rank the shortest stable post-anchor discriminator;
- build a prospective ordered validator only if discovery supports one;
- do not promote the anchor itself.

## P1 — Preserve target/retarget correctness as a release gate

Every production warning must use live Browser target data and recompute side against the current target. A correct attack with stale target/side is a product failure.

## P1 — Refresh COVERAGE after notation normalization

COVERAGE should ingest current SWEEPATLAS/SEQMINER state and normalize identifiers to `T<decimal> (0xHH)` before it drives any Beta/v1 breadth claim or physical recap.

This is an analysis refresh, not a capture request.

## P2 — T23 only when cost-effective coverage exists

T23 ordered evidence remains valuable, but do not spend repeated Browser rooms or broad WinKawaks collection merely to force it to appear.

## Explicit stops

- STOP broad BASECAP collection.
- STOP generic EFIELD mapping.
- STOP generic RAWMINE discovery.
- STOP unlabeled SWEEPATLAS inference.
- STOP treating one-off pair/triple sequences as rules.
- STOP promoting WinKawaks offsets into Browser/WASM.
- STOP using Browser operator time on low-value rare branches while P0 ordered ambiguity/productization remains open.
- DO NOT add another research AI lane.
- PRODUCT/ALPHA is the only newly authorized workstream and must stop at Alpha RC rather than becoming another research program.
