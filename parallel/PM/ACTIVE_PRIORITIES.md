# WOF Future Danger AI — Active Priorities

Updated: 2026-09-01

## P0 — Start Alpha product track now

Freeze a conservative `production` ruleset from rules that already have strong prospective / multi-room production-shadow evidence. Build or harden the user path around that frozen subset:

- reliable loader;
- automatic game-state reading;
- read-only enforcement;
- production-vs-experimental rule isolation;
- fail-closed behavior;
- simple HUD showing danger enemy, target, left/right, and a validated lead-time indication where available;
- UNKNOWN stays silent;
- runtime/version identity guard;
- regression audit for frozen rules.

Do not wait for all enemy types or attacks.

## P0 — MAINLINE ordered T18 discrimination

The exact T18 BODY4728/A4/B2/TM1 anchor is prospectively attack-ambiguous. MAINLINE should continue the WOF-052 ordered-context discovery protocol, then create a prospective validator only for the shortest stable discriminator that separates A4704 from A4712.

Do not promote the anchor by itself.

## P1 — Preserve target/retarget correctness as a release gate

Every production warning must use live target data according to the Browser-proven path and must preserve the active-edge retarget fix. A warning with a correct attack but stale target/side is a product failure.

## P1 — Refresh COVERAGE after notation normalization

COVERAGE should ingest current SWEEPATLAS/SEQMINER outputs and normalize all type identifiers to `T<decimal> (0xHH)`. It should then recompute current evidence density and gaps. This is an analysis refresh, not a new capture request.

## P2 — T23 only when cost-effective coverage exists

T23 ordered evidence is valuable but repeated room hunting has recently produced zero T23 coverage. Keep the T23 A5888 tail and branch-set candidates queued, but do not spend repeated Browser rooms or broad WinKawaks collection merely to force T23 to appear.

## Explicit stops

- STOP broad BASECAP collection.
- STOP generic EFIELD mapping.
- STOP generic RAWMINE discovery.
- STOP unlabeled SWEEPATLAS inference.
- STOP treating one-off pair/triple sequences as rules.
- STOP promoting WinKawaks offsets into Browser/WASM.
- STOP using Browser operator time on low-value rare branches while P0 ordered ambiguity/productization remains open.
- DO NOT open a new AI lane now.
