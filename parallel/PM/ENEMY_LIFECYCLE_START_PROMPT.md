# WOF Enemy Lifecycle / Slot Reuse Audit — canonical launcher

This file exists as the canonical short-path launcher for the dedicated enemy lifecycle work thread.

Read and follow the complete current instructions in:

`parallel/PM/ALPHA_LIFECYCLE_AUDIT_START_PROMPT.md`

Then continue until the stop condition in that file is reached. Treat `product/alpha/**` as read-only and write findings only under `parallel/ALPHALIFE/**`.
