# Alpha V1 Render Authority Owner-Visible Startup Recovery V3 — UX Regression Floor

Status: **AUTHORITATIVE ADDENDUM FOR THE ACTIVE V3 WORKER**

This does not create a new task, claim, recovery, or QA generation. It tightens the already ACTIVE V3 completion criteria after Owner evidence from the V2 package.

## Mandatory no-regression floor

V3 must not trade away previously working Owner-visible product behavior merely to suppress an untrusted overlay coordinate path.

Required:

- restore and retain the known-good Windows right-bottom WOF tray/status indicator or an equivalent persistent visible status surface;
- menu 6 must never leave Owner with only a browser window and no indication whether the collector is STARTING, waiting, locked, tracking, complete, or blocked;
- suppress only untrusted gameplay overlay coordinates/content; do **not** dispose the Owner control/status UX itself;
- do not use `pythonw.exe` as a hidden-only execution path unless the same authoritative state is surfaced continuously through tray/status UI;
- reconnect to/open the correct existing WOF browser flow rather than launching an empty debug browser with no game destination;
- retain automatic evidence ZIP and show completion/blocked result visibly;
- the final successor must include the head-visual acquisition/tracking requirements from `ALPHA_V1_LIVE_ACCEPTANCE_RENDER_AUTHORITY_OWNER_VISIBLE_STARTUP_RECOVERY_V3_SCOPE_CORRECTION.md`.

## Regression rule

Before V3 can COMPLETE, compare the successor Owner path against the last known-good pre-V2 Owner UX. Any loss of tray/status presence, visible progress, re-entry feedback, or actionable one-at-a-time prompts is a product regression and must be fixed in the same V3 module.

The desired product flow remains:

`菜单 6 -> 正常进游戏 -> Camera 自动准备 -> 最多点一次 P1 头顶 -> 自动积累多头部样本 -> 连续跟踪 -> 识别丢失隐藏 -> 恢复后自动显示 -> 自动完成/打包`

Use complete functional-module testing, not step-by-step QA. 少汇报，直接执行。
