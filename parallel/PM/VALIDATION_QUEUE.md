# WOF Future Danger AI — Browser Validation Queue

Updated: 2026-09-01

Browser operator time is expensive. This queue ranks product value, evidence strength and expected information gain.

## P0 — T18 BODY4728 ordered post-anchor discriminator

Why first:
- exact single-state anchor is already prospectively proven forward-relevant but attack-ambiguous;
- it produces A4704 and A4712;
- ordered context is therefore the shortest scientifically justified next step;
- T18 has appeared in recent Browser coverage, unlike T23.

Protocol direction:
1. WOF-052 discovery records candidate-containing attack-zero cycles and ordered distinct context.
2. Rank shortest stable discriminator after the shared anchor.
3. Only then build a direct prospective validator.
4. Require repeated evaluable cycles and report alternate outcomes, misses, target and side.

Promotion gate: prospective repeated separation, then production-shadow audit. Discovery alone never promotes.

## P1 — Frozen Alpha rules regression / release-candidate acceptance

Before Alpha release, run the actual frozen user artifact against the declared supported Browser/game build. Verify:

- loader/identity guard;
- no RAM writes;
- each frozen warning still matches its evidence claim;
- live target/side/retarget;
- UNKNOWN remains silent;
- runtime/HUD clears errors safely.

This is release validation, not new rule discovery.

## P1 — T23 A5888 BODY4936 ordered tail

Candidate:

`S0/A8/B2 BODY4936 -> S0/A2/B0 BODY4936 -> S0/A6/B4 BODY4936`

Do not arm on any constituent state. Validate only the complete ordered tail across multiple evaluable cycles.

Scheduling rule: run when T23 coverage is naturally/cheaply available or a known scene becomes authoritative. Do not repeatedly hunt random rooms merely to force T23.

## P2 — T23 branch-set validator

Build separate repeated branch families for A4792/A4920/A5888/other rather than forcing one universal signature.

## P2 — Cross-target invariance

Any candidate surviving P0/P1 should be repeated across at least two physical targets when coverage permits. If not invariant, keep it as a target-conditioned rule rather than pretending it is universal.

## Deferred / not worth current Browser cost

- T18 BODY4728 single state as A4704-specific rule — falsified.
- any T23 single state spanning multiple attacks.
- T24 zero-coverage audits as proof of failure.
- rare branches without repeated discovery evidence.
- local WinKawaks +0x73/+0x24 structural proxies as Browser attack predictors.

## Queue policy

Priority is:

`common/high-value + repeated discovery + ambiguity resolved + cheap coverage`

before:

`rare + one-off + expensive room hunting`.
