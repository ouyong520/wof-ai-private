# WOF Browser Runtime Identity Audit — canonical launcher

This file exists as the canonical short-path launcher for the dedicated Runtime Identity work thread.

Read and follow the complete current instructions in:

`parallel/PM/ALPHA_RUNTIME_IDENTITY_AUDIT_START_PROMPT.md`

Then continue the audit until the stop condition in that file is reached. Treat `product/alpha/**` as read-only and write findings only under `parallel/ALPHAID/**`.
