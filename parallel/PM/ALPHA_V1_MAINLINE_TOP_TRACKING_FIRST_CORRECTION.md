# Alpha V1 Mainline — Top Tracking First Correction

Owner priority correction: before any danger/business prompt work, complete visible P1 top-of-head tracking as the first product milestone.

## Immediate product scope

Only this path matters now:

`menu 6 -> reuse/open WOF -> acquire P1 head -> production Alpha HUD draws one simple visible P1 head marker -> marker follows P1 head -> tracking loss hides marker -> recovery redraws marker -> re-entry reacquires`

## Explicitly deferred until this milestone is proven

- danger prompts / danger badge semantics
- enemy target business prompts
- P2/P3 product labeling expansion
- new projection research
- manual calibration
- Y / Y-Z / Y+Z experiment revival unless needed later for a narrowly proven sign/direction fix
- zero-click perfection as a blocker for visible tracking (auto first; max one real P1-head click fallback remains allowed)

## Acceptance for the first usable visual milestone

A candidate is not accepted merely because tracker state is HEAD_TRACKING or tests pass. The maintained production Alpha HUD must actually draw a visible marker above P1, with draw evidence, while P1 moves. On loss the marker must disappear rather than freeze or drift; on recovery/re-entry it must return automatically.

The first Owner-facing package must pin the exact runtime that proves this path and must not select any manifest with `productionOverlayEnabled=false`, `productionOverlaySuppressed=true`, or diagnostic-only rendering.

Danger/prompt features are stage 2 and must not delay this first visible-tracking package.
