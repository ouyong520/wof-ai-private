# Alpha V1 Dual-Overlay One-Session Live-Proof Tooling Proof-Authority Fix — Independent Cross-check V2

stageId: `ALPHA_V1_ANCHORED_OVERLAYS_ONE_SESSION_LIVE_PROOF_TOOLING_PROOF_AUTHORITY_FIX_CROSSCHECK_V2`
dedupProtocol: `v2`
dedupKey: `alpha.v1.anchored-overlays.one-session-live-proof-tooling-proof-authority-fix-crosscheck-v2`
dedupMode: `independent-validation`
independentValidationGroup: `alpha.v1.dual-overlay.proof-authority-fix-v1`
independentValidationKey: `second-opinion-v2`

Repository: `ouyong520/wof-ai-private`

This is PM-authorized second-opinion validation and may coexist with the Fresh QA V2. It is not an accidental duplicate.

Do not use the implementation regression or Fresh QA fixture/verdict as proof. Build an independent attack model focused on whether the fix itself can still be bypassed.

Attack at minimum:

- can any repository/synthetic JSON/event stream mint or smuggle the internal live capability;
- witness replay across proof session, Worker generation, runtime epoch, pair nonce or formal adapter authority tuple;
- malformed/partial witness handling;
- cross-epoch player+enemy evidence closing a joint phase;
- same-slot replacement represented as retarget;
- player respawn preserving old calibration;
- enemy type offset surviving occupant replacement;
- stale stop/reinstall completion without the exact transaction witness;
- old drawing-buffer/mapping epoch combined with fresh surface evidence;
- malformed/coercible `warningSampleAt`, target and epoch fields;
- terminal/result serialization accidentally upgrading synthetic evidence;
- RUN_MANIFEST drift and stale pinned proof blobs;
- safety boundary remains read-only with zero RAM writes and no gameplay input injection.

No Browser/WOF. Repository-only cross-check. Do not modify implementation.

PASS:
`PASS — ALPHA V1 DUAL-OVERLAY PROOF-AUTHORITY FIX INDEPENDENT CROSS-CHECK V2 — NO FALSE-PROOF BYPASS FOUND`

BLOCKED:
`BLOCKED — ALPHA V1 DUAL-OVERLAY PROOF-AUTHORITY FIX INDEPENDENT CROSS-CHECK V2 — <precise deterministic bypass>`
