# Alpha V1 Product Takeover — First Owner Gate Parallel 3-Worker V2 Dispatch

Repository: `ouyong520/wof-ai-private`

Authority baseline: `cd754f9e84ec23c1ab36fbdc6572f91a2de131d7`

Scope: **Alpha Owner-visible product only**. Collector / Unified Collector / Training Farm / 10训 are out of scope.

## Supersession

The prior unclaimed prompts below were never acquired and never executed:

- `ALPHA_V1_PRODUCT_TAKEOVER_G1_FIXED_DRAW_OWNER_GATE_RUNTIME_WIRING_START_PROMPT.md`
- `ALPHA_V1_PRODUCT_TAKEOVER_G2_FIRST_OWNER_GATE_BOOTSTRAP_HANDOFF_START_PROMPT.md`
- `ALPHA_V1_PRODUCT_TAKEOVER_G3_FIRST_OWNER_GATE_INTEGRATION_PROMOTION_START_PROMPT.md`

They are superseded-before-claim by this dispatch. Do not start or claim them. This is not a recovery because no ownership existed.

W1 permanent bootstrap and W2 maintained fixed-draw smoke remain accepted implementation-ready inputs. W3 renderer/object authority remains SUBCOMPLETE under its existing ACTIVE authority and is untouched.

Current product objective is still exactly one first Owner gate:

`permanent WOF_ALPHA_TEST.cmd -> controlled alpha-live candidate -> maintained production WebGL fixed TEST -> precise feedback -> one real-WOF visual observation`

No P1/enemy/semantic/zero-click/danger expansion is authorized.

## P1 — Runtime fixed TEST gate activation

Start prompt:
`parallel/PM/ALPHA_V1_PRODUCT_TAKEOVER_P1_RUNTIME_FIXED_TEST_GATE_START_PROMPT.md`

Owns runtime gate activation only. When a controlled environment flag is present, the normal Alpha runtime must enable and poll the existing `ProductionHudFixedDrawSmoke` before any P1 acquisition dependency. It writes raw machine-readable fixed-smoke state into `Documents\WOF_RESULTS`. It does not edit updater/bootstrap or HUD smoke internals.

## P2 — Permanent launcher controlled gate mode

Start prompt:
`parallel/PM/ALPHA_V1_PRODUCT_TAKEOVER_P2_PERMANENT_LAUNCHER_GATE_MODE_START_PROMPT.md`

Owns live-mode handoff only. The permanent updater/launcher reads one repository-controlled first-gate marker and automatically sets the runtime gate flag. Owner never sets env vars or chooses a mode. It preserves zero-state bootstrap, SSH/22, `alpha-live`, one permanent launcher, and Browser/WOF reuse. It does not edit runtime probe logic or HUD.

## P3 — Owner feedback + acceptance harness

Start prompt:
`parallel/PM/ALPHA_V1_PRODUCT_TAKEOVER_P3_OWNER_FEEDBACK_ACCEPTANCE_HARNESS_START_PROMPT.md`

Owns tests/feedback schema/readiness tooling only. It creates one deterministic first-gate acceptance harness and a clear feedback aggregation contract so PM can distinguish bootstrap/update failure from runtime/HUD/draw failure without asking Owner to use DevTools. It does not edit P1/P2 production files and does not move `alpha-live`.

## File separation

- P1: runtime entry/gate glue + P1-specific focused test/result files only.
- P2: updater/bootstrap/live-mode marker + P2-specific focused test/result files only.
- P3: tests/fixtures/feedback helper/schema/docs/result only; no HUD/runtime/updater production edits.
- Nobody edits W3 renderer/object authority or its ACTIVE claim.
- Nobody moves `alpha-live` in this parallel round.

## PM integration after all three return

PM will consume exact P1/P2/P3 commits, run one coherent focused integration pass, and only then promote the accepted candidate to `alpha-live`.

Do not ask Owner to test until PM explicitly declares `READY_FOR_OWNER_FIXED_TEST`.

The first Owner question remains only:

**`真实 WOF 游戏画面里，固定 TEST 是否持续显示？`**
