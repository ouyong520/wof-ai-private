# Alpha V3 — Integration Resume Prompt

Status: **CONTINUE EXISTING ACTIVE V3 — NO NEW RECOVERY / NO NEW CLAIM**

Continue the existing active task:

`parallel/PM/ALPHA_V1_LIVE_ACCEPTANCE_RENDER_AUTHORITY_OWNER_VISIBLE_STARTUP_RECOVERY_V3_START_PROMPT.md`

and consume the authoritative addenda/corrections already on main, especially:

- `parallel/PM/ALPHA_V1_LIVE_ACCEPTANCE_RENDER_AUTHORITY_OWNER_VISIBLE_STARTUP_RECOVERY_V3_SCOPE_CORRECTION.md`
- `parallel/PM/ALPHA_V1_LIVE_ACCEPTANCE_RENDER_AUTHORITY_OWNER_VISIBLE_STARTUP_RECOVERY_V3_UX_REGRESSION_FLOOR.md`
- `parallel/PM/ALPHA_V1_LIVE_ACCEPTANCE_RENDER_AUTHORITY_OWNER_VISIBLE_STARTUP_RECOVERY_V3_ZERO_CLICK_DEFECT_CORRECTION.md`
- `parallel/PM/ALPHA_V1_LIVE_ACCEPTANCE_RENDER_AUTHORITY_OWNER_VISIBLE_STARTUP_RECOVERY_V3_W2_W3_INTEGRATION_CLOSEOUT_CORRECTION.md`

Current authority is already restored ACTIVE because the prior closeout was premature. Do not acquire or create another umbrella claim and do not create V4.

## Existing accepted work — do not redo

- W2 zero-click identity acquisition module is SUBCOMPLETE/integration-ready.
- W3 zero-click acceptance/package gate is SUBCOMPLETE and its claims are already closed.
- Current V3 already has tray/status restoration, browser/WOF reuse, canvas screenshot support, head visual tracker, multi-template tracking, zero-click-first inline candidate search, one-click fallback, and package plumbing.

## Only remaining production defect

`head_visual_tracker.py` must truly consume the W2 fail-closed semantic identity/evidence adjudicator before zero-click automatic binding. Merely packaging `zero_click_identity_acquisition.py` is insufficient.

Do not certify a P1 identity/head seed from a generic colorful HUD palette crop. Reuse real proven HUD/portrait/tile/render identity evidence already available in the Alpha/runtime stack when it exists. Bind exact World 921031 + current P1 lifecycle/type/generation + current canvas digest + semantic HUD identity + bounded scene P1/head evidence through W2. Only `SAFE_UNIQUE` may enter `HEAD_TRACKING` with `ownerClickCount=0`.

If semantic identity is unavailable/ambiguous, fail closed and only then allow the existing one-click scene-P1-head fallback. Runtime/lifecycle/layout change must revoke stale identity/head authority and reacquire from zero.

## One final closeout pass

After the real integration is complete, run only one coherent focused gate:

1. W2 focused identity tests;
2. V3 zero-click tracker regression;
3. W3 fixture gate;
4. W3 candidate/package-readiness mode against the final generated candidate;
5. verify final immutable manifest/blob pins select the integrated runtime.

Do not rerun unrelated historical PASS suites.

Then regenerate the immutable successor, write durable V3 RESULT, and close the existing V3 canonical/stage claims COMPLETE. Do not send Owner any intermediate package.

Terminal states allowed: COMPLETE or precise external/real semantic-evidence BLOCKED.
