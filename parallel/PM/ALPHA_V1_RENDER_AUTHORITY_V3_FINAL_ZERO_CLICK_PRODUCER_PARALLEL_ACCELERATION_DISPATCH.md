# Alpha V3 — Final Zero-Click Producer Parallel Acceleration Dispatch

Status: **AUTHORITATIVE PARALLEL ACCELERATION ADDENDUM — SAME ACTIVE V3 UMBRELLA**

This dispatch does not create a new Alpha recovery generation. The existing V3 umbrella canonical/stage claims remain authoritative and ACTIVE. Existing W2/W3 outputs are accepted. Existing W4 remains ACTIVE and must not be duplicated.

## Current durable finding

The V3 W2 integration consumer/gate is now real and the focused closeout workflow has passed, but the selected exact-runtime capture worker currently does not emit `p1ZeroClickEvidence`. Therefore a package can currently be fail-closed safe while still lacking the producer needed for the intended normal zero-click path.

This is not sufficient for product closeout. Default V1 behavior remains:

`menu 6 -> exact World -> semantic P1 identity -> scene P1/head -> HEAD_TRACKING with ownerClickCount=0`

One click is fallback only after a real automatic semantic acquisition attempt fails safely.

## Existing active/accepted workstreams

- V3 umbrella: ACTIVE; owns final integration/package/RESULT/claims.
- W2: SUBCOMPLETE/accepted; fail-closed acquisition consumer contract exists.
- W3: COMPLETE/accepted; existing fixture/package gate exists, but it must not be treated as sufficient evidence that a semantic evidence producer exists.
- W4: ACTIVE; read-only search for reusable semantic P1 identity authority. Do not duplicate W4 source-discovery work.

## Parallel split for three newly freed workers

### Main / terminal coordinator — existing umbrella, no new subclaim

Continue current V3 umbrella. Do not close yet.

Responsibilities:
1. Keep the existing W2 consumer integration and passed focused gate.
2. Consume W4 durable result when available.
3. Consume W6 producer module when available.
4. Wire only proven W4 authority into W6 producer, then feed the resulting explicit evidence envelope to the existing W2 fail-closed consumer.
5. Re-run one coherent final focused regression only after integration settles.
6. Require W5 producer-readiness acceptance to pass.
7. Generate final immutable package from the actual integrated runtime, durable RESULT, then close existing V3 canonical/stage COMPLETE.

Do not send the current intermediate package to Owner.

### W5 — Producer-readiness acceptance / false-green prevention

New subworkstream. Tests/fixtures/docs only; no production edits, no package publication, no umbrella claim mutation.

Goal: prevent a candidate from passing merely because a W2 consumer exists while no selected runtime path can ever produce semantic zero-click evidence.

Required checks:
- automatic zero-click success fixture requires an actual semantic-evidence producer path, not only a consumer/gate;
- current selected runtime with no `p1ZeroClickEvidence` producer must fail candidate readiness for the normal zero-click claim;
- fail-closed one-click fallback remains allowed as safety behavior but cannot satisfy `ownerClickExpectedNormal=0` product readiness by itself;
- generic HUD palette / runtime-type copying cannot satisfy producer readiness;
- producer output must be runtime/lifecycle/layout generation bound and read-only;
- package manifest/blob pins must select both producer and consumer integration runtime.

Use deterministic execution/behavior checks; avoid brittle source-string assertions where a runtime/fixture oracle is practical.

Suggested identifiers:
- dedupKey: `alpha.v1.render-authority-v3.w5-zero-click-producer-readiness`
- stageId: `ALPHA_V1_RENDER_AUTHORITY_V3_W5_ZERO_CLICK_PRODUCER_READINESS`

Terminal: SUBCOMPLETE with durable SUBRESULT, or precise BLOCKED.

### W6 — Independent semantic evidence producer module

New subworkstream. Integration-ready producer module + focused tests only. Do not modify `head_visual_tracker.py`, `measurement_runner.py`, Owner UI, package manifest, or V3 umbrella claims.

Goal: implement a strict producer contract that converts already-proven semantic observations into the explicit `p1ZeroClickEvidence` envelope consumed by V3/W2. W6 does **not** own discovery of where the semantic observation comes from; W4 owns that.

Producer requirements:
- exact World 921031 binding;
- P1 lifecycle active + generation + runtime character type;
- semantic identity input must be explicitly sourced from HUD/portrait/tile/render authority and independently identify the character; never manufacture it by copying runtime P1 type;
- scene P1/head candidate must be actor=P1, generation-bound, bounded, confidence/margin-carrying, and backed by canvas + verified sprite/tile/render-object evidence or an explicitly proven coarse-prior consistency signal;
- ambiguity/mismatch/stale generation -> emit no semantic evidence envelope;
- output schema compatible with the existing V3 `ZERO_CLICK_EVIDENCE_SCHEMA` / W2 consumer expectations;
- readOnly=true, ramWrites=0, inputInjection=false;
- no ROM/game content committed.

Prefer a small standalone module with deterministic fixtures/tests so umbrella integration is a narrow adapter step after W4 identifies the real source.

Suggested identifiers:
- dedupKey: `alpha.v1.render-authority-v3.w6-semantic-evidence-producer`
- stageId: `ALPHA_V1_RENDER_AUTHORITY_V3_W6_SEMANTIC_EVIDENCE_PRODUCER`

Terminal: SUBCOMPLETE with durable SUBRESULT, or precise BLOCKED.

## File/authority boundaries

- W4: read-only discovery only.
- W5: tests/fixtures/docs/SUBRESULT only.
- W6: standalone producer module + its own tests/fixtures/SUBRESULT only.
- Umbrella/main: integration files, package, RESULT, umbrella claims.

No worker may steal another worker's claim or edit the same production file concurrently.

## Final closeout condition

V3 may close only when all are true:

1. real producer exists and is selected in final runtime;
2. real semantic authority source is non-circular and proven;
3. W2 consumer allows zero-click binding only on SAFE_UNIQUE;
4. safe unique fixture reaches HEAD_TRACKING with ownerClickCount=0;
5. ambiguity/missing evidence fails closed and only then permits at most one click;
6. W5 producer-readiness gate passes;
7. W2 + V3 tracker + W3 + W5 candidate gate pass once on final source;
8. immutable package pins the final producer+consumer runtime;
9. durable V3 RESULT records the remaining real-WOF-only assertions without fabricating live PASS.
