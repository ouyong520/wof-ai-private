# Alpha V1 Canonical Product Convergence — P15 + W3 + P16 Long 3-Worker Dispatch

Repository: `ouyong520/wof-ai-private`

Purpose: use three substantial, non-overlapping implementation streams instead of micro-tasks.

## Worker 1 — P15 Canonical Product Convergence + Package Candidate

Authority:
`parallel/PM/ALPHA_V1_PRODUCT_TAKEOVER_P15_CANONICAL_PRODUCT_CONVERGENCE_PACKAGE_CANDIDATE_START_PROMPT.md`

Owns canonical product convergence, absorbed P14 semantic/spatial decoupling, P12->P10->P9/P8/P11 normal AlphaRuntime lifecycle integration, truthful runtime status production, and package candidate pinning. Does not move alpha-live or qualify W3 source.

Primary ownership includes `alpha_runtime.py`, any P15 canonical coordinator, field-adapter semantic decoupling, and package refresh/manifest candidate files.

## Worker 2 — W3 existing ACTIVE claim long renderer qualification continuation

Authority:
`parallel/PM/ALPHA_V1_W3_ACTIVE_CLAIM_LONG_QUALIFICATION_CONTINUATION.md`

This slot reattaches to the existing ACTIVE W3 claim. It must not create/recover/steal a claim. It owns W3 renderer-source reverse trace, deterministic qualification analyzer, bounded normal-play capture readiness, and W3 producer qualification evidence under W3-owned files.

It must remain fail-closed if exact displayed-frame renderer/object source cannot be proven without the later bounded Owner run.

## Worker 3 — P16 Owner Canonical Status + Acceptance Evidence

Authority:
`parallel/PM/ALPHA_V1_PRODUCT_TAKEOVER_P16_OWNER_CANONICAL_STATUS_ACCEPTANCE_EVIDENCE_START_PROMPT.md`

Owns the launcher-side Owner state normalization, bounded significant canonical transition evidence, tray/diagnostics UX, and automatic final-acceptance evidence snapshot. It consumes runtime status only and never becomes position authority.

Primary ownership includes `monitor.py`, `state.py`, `tray.py`, and new narrow Owner status/evidence helpers. It must not edit P15 runtime/package files or W3 files.

## Parallel boundaries

These three streams are deliberately separated:
- P15: runtime convergence + semantic/spatial split + package candidate.
- W3: renderer/object source qualification and evidence machinery.
- P16: Owner-facing status/evidence consumer surface.

No worker may move `alpha-live` or claim real-WOF visible PASS in this dispatch. No broad Fresh QA. Implementation first, focused self-checks only.

Safety remains `readOnly=true`, `ramWrites=0`, `inputInjection=false`.

After all terminal results, PM integrates the candidate, resolves any narrow interface mismatch, then performs one concentrated final acceptance round and the bounded Owner real-WOF gate.